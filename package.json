{
  "name": "nha-minh",
  "version": "0.1.0",
  "private": true,
  "description": "Nhà mình — nơi hai người cùng nhớ việc nhà, sự kiện hai bên, khoản tiền và giấy tờ quan trọng",
  "scripts": {
    "test": "pnpm --filter @family-organizer/domain test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "mobile": "pnpm --filter @family-organizer/mobile start",
    "db:push": "supabase db push",
    "db:pull": "supabase db pull",
    "fn:deploy": "supabase functions deploy --import-map supabase/import_map.json generate-task-instances build-reminders refresh-lunar-dates presign-upload confirm-upload sign-download spawn-debt-installments expire-attention-items nudge-snapshot-update autosnapshot-monthly sweep-orphan-uploads purge-soft-deleted"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.18.0"
}
